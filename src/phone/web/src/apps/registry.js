import Settings from './settings/main.vue'
import settingsIconUrl from './settings/icon-settings.png'

export default {
    installed: [
        {
            name: "Settings",
            component: Settings,
            icon: settingsIconUrl
        }
    ]
}
